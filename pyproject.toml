[project]
name = "viya-monitor"
version = "0.1.0"
description = "SAS Viya 存储及其他指标采集 agent，每日上报至 ClickHouse"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "pyyaml>=6.0",
]

[project.scripts]
viya-monitor = "src.main:main"
